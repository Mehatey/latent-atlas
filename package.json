{
  "name": "latent-atlas",
  "version": "0.1.0",
  "description": "An explorable galaxy of a real museum collection, embedded with CLIP and rendered in WebGPU. Type a phrase and a model running in your browser flies you to the matching art by meaning.",
  "type": "module",
  "scripts": {
    "atlas": "node --import tsx scripts/build-atlas.ts",
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "check": "npm run typecheck && npm run build",
    "preview": "vite preview",
    "server": "node --import tsx server/index.ts",
    "dev:full": "concurrently -n vite,api -c cyan,yellow \"npm run dev\" \"npm run server\""
  },
  "dependencies": {
    "@huggingface/transformers": "^3.3.3",
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@fastify/static": "^10.1.0",
    "@types/three": "^0.171.0",
    "concurrently": "^10.0.3",
    "fastify": "^5.2.1",
    "puppeteer": "^25.1.0",
    "sharp": "^0.33.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "umap-js": "^1.4.0",
    "vite": "^6.0.7"
  }
}
